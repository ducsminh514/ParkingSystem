@page "/customers/{CustomerId:guid}"
@using ParkingSystem.Shared.Models
@using ParkingSystem.Client.Services
@inject CustomerService CustomerService
@inject NavigationManager NavigationManager

<PageTitle>Customer Details</PageTitle>

<div class="container-fluid">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/customers">Customers</a>
            </li>
            <li class="breadcrumb-item active">Details</li>
        </ol>
    </nav>

    @if (isLoading)
    {
        <div class="text-center py-5">
            <div class="spinner-border text-primary"></div>
            <p class="mt-2">Loading...</p>
        </div>
    }
    else if (customer == null)
    {
        <div class="alert alert-warning">
            Customer not found
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5><i class="bi bi-person-circle"></i> Customer Information</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Full Name:</strong><br />@customer.FullName</p>
                        <p><strong>Phone Number:</strong><br />@customer.Phone</p>
                        <p><strong>Email:</strong><br />@(customer.Email ?? "N/A")</p>
                        <p><strong>ID:</strong><br /><small class="text-muted">@customer.CustomerId</small></p>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="bi bi-car-front-fill"></i> Vehicle List</h5>
                    </div>
                    <div class="card-body">
                        @if (customer.Vehicles.Any())
                        {
                            <div class="table-responsive">
                                <table class="table table-sm">
                                    <thead>
                                        <tr>
                                            <th>Plate Number</th>
                                            <th>Vehicle Type</th>
                                            <th>Number of Registrations</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var vehicle in customer.Vehicles)
                                        {
                                            <tr>
                                                <td><strong>@vehicle.PlateNumber</strong></td>
                                                <td>@vehicle.VehicleType</td>
                                                <td>
                                                    <span class="badge bg-info">
                                                        @vehicle.ParkingRegistrations.Count times
                                                    </span>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <p class="text-muted">No vehicles registered</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@code {
    [Parameter]
    public Guid CustomerId { get; set; }

    private Customer? customer;
    private bool isLoading = true;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            customer = await CustomerService.GetCustomerWithFullDetailsAsync(CustomerId);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }
        finally
        {
            isLoading = false;
        }
    }
}
